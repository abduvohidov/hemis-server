generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgres"
  url      = "postgres://postgres:AB2091008uzb@localhost:5432/hemis_database"
}

// #test
model UserModel {
  id    Int     @id @default(autoincrement())
  password String?
  email String  @unique
  name  String
}