generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgres"
  url      = env("DATABASE_URL")
}

model UserModel {
  id       String @id @default(uuid())
  email    String
  password String?
  name     String
}
