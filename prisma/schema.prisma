generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgres"
  url      = env("DATABASE_URL")
}

// #test
model UserModel {
  id    Int     @id @default(autoincrement())
  password String
  email String  @unique
  name  String
}